project_id = "iqjerqtcquvchvbseveh"

[functions.enrich]
# The function reads the caller's JWT itself and builds an RLS-scoped client
# from it, so the platform's own JWT gate stays on.
verify_jwt = true

[functions.product-data]
verify_jwt = false

[functions.ingest]
# Called by a phone automation carrying a per-user ingest token, not a JWT.
verify_jwt = false
